# Configurações do Projeto - Tech Challenge Fase 4
# Copie este arquivo para .env e preencha os valores

# OpenAI API (opcional - para geração de resumo com LLM)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Caminho do vídeo de entrada
VIDEO_PATH=./input/video.mp4

# ===== CONFIGURAÇÕES DE GPU =====
# Valores: "auto" (detecta), "true" (força GPU), "false" (força CPU)
USE_GPU=auto

# ===== CONFIGURAÇÕES DE PROCESSAMENTO =====
FRAME_SKIP=2              # Processar 1 a cada N frames (1=todos, 5=rápido)
CONFIDENCE_THRESHOLD=0.5  # Threshold mínimo de confiança para detecções
TARGET_FPS=30             # FPS do vídeo de saída
ENABLE_PREVIEW=true       # Preview em tempo real durante processamento
PREVIEW_FPS=10            # FPS do preview (menor = menos uso de CPU)

# ===== DETECTORES AVANÇADOS =====
# Ativa/desativa módulos extras (impacta performance)
ENABLE_OBJECT_DETECTION=true    # Detecta objetos fora de contexto (YOLO11)
ENABLE_OVERLAY_DETECTION=false  # Detecta watermarks/texto (OCR) - requer easyocr
ENABLE_SEGMENT_VALIDATION=false # Valida silhuetas humanas (YOLO11-seg) - mais lento

# Tamanho dos modelos YOLO: n(ano), s(mall), m(edium), l(arge), x
# Maior = mais preciso, mais lento
YOLO_MODEL_SIZE=n
